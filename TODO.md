# 남은 작업

요청한 기능들의 현재 상태와, 아직 못 한 것들을 정리해 둔다.

## 기능별 진행 상황

| # | 기능 | 상태 | 비고 |
|---|------|------|------|
| 1 | 캐릭터 커스텀 / 시트 애니메이션 | 대부분 완료 | 시트 등록이 아직 수동 (아래 참고) |
| 2 | 진행 중인 채팅 보여주기 | 완료 | 말풍선에 제목(또는 답변/질문 요약) + 상태 |
| 3 | VSCode 채팅 연동 | 완료 | `entrypoint`로 출처 구분, 세션별 파일 분리 |
| 4 | 답변 도착 시 말풍선에 표시 | 완료 | 실제 실행 화면으로 확인함 |
| 5 | 펫 크기 조절 | 완료 | 트레이 메뉴 + 펫 클릭 → 설정 패널 |
| 6 | 화면/작업표시줄 밖으로 못 나가게 | 완료 | 펫의 실제 위치 기준으로 clamp (아래 참고) |
| 7 | 펫 클릭 → 설정 패널 | 구현 완료 · 실기기 확인 필요 | 아래 참고 |

## 아직 못 한 것

### 1. 펫 클릭 → 설정 패널, 실제 클릭으로 재확인 필요
펫을 누르면 아래에 설정 패널(크기 +/−, 캐릭터 선택)이 뜨도록 구현했다
(`renderer/renderer.js`의 `setPanelVisible`, `mousedown`/`mouseup` 클릭·드래그 판정).
자동화된 마우스 클릭으로 검증을 시도했는데, 이 오버레이 창 특성상
(투명·항상 위·클릭 통과) 프로그래밍적 마우스 이벤트가 불안정하게 전달돼
확실히 확인하지 못했다 — 한 번은 `mousedown`이 정확히 펫 위에서 잡혔는데도
그 사이 미세한 이동이 섞여 드래그로 판정되기도 했다. 코드 로직 자체는
검토했을 때 맞아 보이지만, **실제 손으로 클릭해서 패널이 뜨는지 확인 필요**.
안 뜨면 알려달라 — `CLICK_DRAG_THRESHOLD`(현재 6px)를 조정하거나 판정
로직을 더 봐야 할 수 있다.

### 2. 커스텀 시트 등록 UI
지금은 새 캐릭터를 넣으려면 손으로 두 단계를 거쳐야 한다.

```
1) assets/characters/<이름>-sheet.webp 로 시트 이미지를 넣는다
2) tools/analyze-sheet.html, tools/preview-rows.html 안의 SHEET 상수를
   그 파일명으로 고친다
3) npx electron tools/analyze-sheet.js              # 프레임 격자 자동 검출
   npx electron tools/preview-rows.js               # 어떤 행이 어떤 동작인지 눈으로 확인
   node tools/build-sheet-json.js <id> <이름> <파일명>  # assets/characters/<id>.json 생성
```

행 구성이 지금까지 받은 시트들과 다르면 `tools/build-sheet-json.js`의
`CLIPS` / `STATES` 상수도 고쳐야 한다.
→ 이걸 앱 안의 설정 창에서 "이미지 끌어다 놓기 → 행별 동작 고르기"로 할 수 있게
만드는 게 남았다. (2번 단계의 SHEET 상수 수정도 인자로 받게 하면 더 낫다.)

### 3. 설정 창(GUI)
현재 설정은 전부 트레이 메뉴에만 있다. 크기 슬라이더, 캐릭터 미리보기,
말풍선 표시 시간 같은 걸 조절할 창이 없다.

### 4. ~~시트 원본 해상도로 교체~~ → 완료
`mong-sheet.webp`로 교체했다 (1536x2288, 무손실 VP8L). 이전에 대화에
붙여넣었던 `bichon-sheet.webp`(1343x2000, 손실 압축 사본)는 지웠다.

### 5. 안 쓰고 있는 클립 활용
시트에 있는 11개 동작 중 4개만 상태에 물려 있다.
남은 것: `run`(달리기), `beg`(조르기), `sleep`(엎드려 잠들기), `happy`(신남),
`sit`(앉아 방긋), `turn`(한 바퀴), `trot`(작게 총총).

쓸 만한 곳:
- 오래 대기하면 `sleep`으로 전환
- 답변이 막 도착했을 때 `happy` 한 번 재생
- 긴 작업이면 `walk` 대신 `run`

### 6. 말풍선에서 답변 전체 보기
지금은 5줄로 잘리고 20초 뒤 접힌다. 클릭하면 전체를 스크롤해서 보는 기능이 없다.

### 7. 아이콘 교체
`assets/icons/tray.png`, `app-icon.png`이 아직 예전 것이다. 몽이로 교체 필요.

### 8. 확인 안 된 것들
- Windows에서만 실행해 봤다. macOS / Linux 미확인.
- `npm run dist` (electron-builder 패키징) 미확인.
- 세션이 3개 이상일 때 말풍선 아래 점 클릭 전환이 잘 도는지 미확인.

## 참고: 개발 중 걸렸던 것

이 환경에는 `ELECTRON_RUN_AS_NODE=1`이 설정돼 있어서 `npx electron .`이
GUI 없이 순수 Node로 돌아버린다. 앱이나 `tools/` 스크립트를 실행할 땐
아래처럼 환경 변수를 빼고 실행해야 한다.

```
env -u ELECTRON_RUN_AS_NODE npx electron .
```
