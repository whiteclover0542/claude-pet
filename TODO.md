# 남은 작업

요청한 기능들의 현재 상태와, 아직 못 한 것들을 정리해 둔다.

## 기능별 진행 상황

| # | 기능 | 상태 | 비고 |
|---|------|------|------|
| 1 | 캐릭터 커스텀 / 시트 애니메이션 | 대부분 완료 | 시트 등록이 아직 수동 (아래 참고) |
| 2 | 진행 중인 채팅 보여주기 | 완료 | 말풍선에 제목(또는 답변/질문 요약) + 상태 |
| 3 | VSCode 채팅 연동 | 완료 | `entrypoint`로 출처 구분, 세션별 파일 분리 |
| 4 | 답변 도착 시 말풍선에 표시 | 완료 | 실제 실행 화면으로 확인함 |
| 5 | 펫 크기 조절 | 완료 | 트레이 메뉴 + 펫 클릭 → 톱니바퀴 → 설정 패널 |
| 6 | 화면/작업표시줄 밖으로 못 나가게 | 완료 | 펫의 실제 위치 기준으로 clamp |
| 7 | 펫 클릭 → 설정 패널 | 완료 | 아래 참고 |
| 8 | 트레이 없이도 연동/종료 | 완료 | 아래 참고 |
| 9 | 안 쓰던 클립 활용 | 완료 | 아래 참고 |
| 10 | 시트 눈/코 결함 수정 | 완료 | 아래 참고 |
| 11 | 듀얼 모니터로 드래그 안 되던 것 | 완료 | 아래 참고 |
| 12 | 말풍선 답변 본문 제거 | 완료 | 아래 참고 |
| 13 | Claude Code 실행 시 자동 실행 | 완료 | 아래 참고 |
| 14 | 클립 우선순위 재구성(드래그·마우스 각도) | 완료 | 아래 참고 |
| 15 | 시트 마젠타 잔여물 제거 | 완료 | 아래 참고 |
| 16 | 모션 속도 조절 | 완료 | 전체적으로 fps를 20~40% 낮췄다 |
| 17 | always-on-top이 게임에 밀리는 문제 | 부분 개선 | 아래 참고 (근본적 한계 있음) |

## 아직 못 한 것

### 1. ~~펫 클릭 → 설정 패널~~ → 완료, 2단계 UX로 변경
처음엔 펫을 누르면 바로 패널이 열리게 했는데, 실수로 열리는 걸 막기 위해
"펫 클릭 → 톱니바퀴 노출 → 그걸 눌러야 패널이 열림"으로 바꿨다
(`renderer/renderer.js`의 `setGearVisible` / `setPanelVisible`).

자동화된 OS 마우스 클릭으로는 이 오버레이 창(투명·항상 위·클릭 통과)에
프로그래밍적 이벤트가 불안정하게 전달돼 검증이 안 됐지만, 앱을 실제로
띄운 채 페이지 안에서 `MouseEvent`를 직접 `dispatchEvent`하는 방식으로
전체 시나리오(펫 클릭→톱니 노출, 톱니 클릭→패널 열림, 크기 +/−, 바깥
클릭→둘 다 닫힘, 드래그는 톱니가 안 열림)를 검증했고, 사용자가 실제
손으로 클릭해서 전부 정상 동작하는 것까지 확인했다. ✅

**트레이 아이콘이 안 보이는 문제**도 같이 발견했다 — `Tray.getBounds()`로
확인해 보면 Windows에 등록은 되어 있는데(좌표도 정상 반환) 화면에
렌더링되지 않았다. 이 세션이 원격 제어(원격 데스크톱류) 환경이라 그
특성상 트레이 렌더링이 막힌 것으로 추정된다(앱 코드 문제는 아닌 듯).
트레이에만 있던 핵심 기능(Claude Code 연동 설정, 종료)을 설정 패널에도
넣어서, 트레이가 안 보이는 환경에서도 완전히 쓸 수 있게 했다.

### 2. 커스텀 시트 등록 UI
지금은 새 캐릭터를 넣으려면 손으로 두 단계를 거쳐야 한다.

```
1) assets/characters/<이름>-sheet.webp 로 시트 이미지를 넣는다
2) tools/analyze-sheet.html, tools/preview-rows.html 안의 SHEET 상수를
   그 파일명으로 고친다
3) npx electron tools/analyze-sheet.js              # 프레임 격자 자동 검출
   npx electron tools/preview-rows.js               # 어떤 행이 어떤 동작인지 눈으로 확인
   node tools/build-sheet-json.js <id> <이름> <파일명>  # assets/characters/<id>.json 생성
```

행 구성이 지금까지 받은 시트들과 다르면 `tools/build-sheet-json.js`의
`CLIPS` / `STATES` 상수도 고쳐야 한다.
→ 이걸 앱 안의 설정 창에서 "이미지 끌어다 놓기 → 행별 동작 고르기"로 할 수 있게
만드는 게 남았다. (2번 단계의 SHEET 상수 수정도 인자로 받게 하면 더 낫다.)

### 3. 설정 창(GUI)
현재 설정은 전부 트레이 메뉴에만 있다. 크기 슬라이더, 캐릭터 미리보기,
말풍선 표시 시간 같은 걸 조절할 창이 없다.

### 4. ~~시트 원본 해상도로 교체~~ → 완료
`mong-sheet.webp`로 교체했다 (1536x2288, 무손실 VP8L). 이전에 대화에
붙여넣었던 `bichon-sheet.webp`(1343x2000, 손실 압축 사본)는 지웠다.

### 5. ~~안 쓰고 있는 클립 활용~~ → 완료
처음엔 자세만 보고 클립 이름을 추측했는데(`beg`, `tilt`, `turn`, `sleep` 등),
실제 만든 사람이 각 행의 의도를 알려줘서 완전히 다시 확인하고 이름을
바로잡았다. 11개 전부 확정된 이름:

`idle`(기본), `walkRight`/`walkLeft`(좌우 걷기), `wave`(인사), `jump`(점프 후
착지), `sulky`(시무룩), `poke`(앞발로 건드리기), `working`(작업하는 상태),
`ponder`(고민하는 상태), `lookRight`/`lookLeft`(마우스 커서 바라보기).

이제 상태에 그냥 고정 매핑하지 않고 "경과 시간·이벤트·마우스 위치"에 따라
자연스럽게 갈아타도록 상태 머신을 만들었다 (`renderer/renderer.js`의
`updateClip`).

- `working` 상태에 이제 `walk`가 아니라 진짜 `working`(작업하는 상태) 클립을 쓴다
- `thinking` 상태는 `ponder`(고민하는 상태) — 자세와 의미가 정확히 맞다
- 대기가 60초 넘게 이어지면 `idle` → `sulky`(시무룩)
- 답변이 막 도착하면 3.5초간 `jump`를 최우선으로 재생한 뒤 원래 상태로 복귀
- 작업이 15초 넘게 이어지면 `working` → `poke`(더 적극적으로 건드림)로 전환
- 여러 세션 중 점을 눌러 전환하면 `walkRight`/`walkLeft` 중 하나를 1.1초 보여준 뒤 복귀
- **신규**: 펫에 마우스를 올리면 커서가 있는 쪽(`lookLeft`/`lookRight`)을 바라본다.
  이 시트에만 있는 리소스라 상태 머신과 별개로 마우스 호버 이벤트에 연결했다

검증하면서 두 가지를 더 고쳤다:
- **`setInterval`이 이 창(포커스 없음·항상 위)에서 거의 안 도는 문제**를
  발견해서, 이미 정상 동작이 확인된 `requestAnimationFrame` 루프 안에서
  500ms 주기를 재는 방식으로 바꿨다 (`clipTickLoop`).
- `backgroundThrottling: false`를 창 옵션에 추가해 애니메이션·타이머가
  포커스 없이도 정상 속도로 돌게 했다.

실제 몇 분씩 기다리지 않고, 앱을 띄운 채 페이지 안에서 `idleSince`/
`workingSince` 등을 직접 과거 시각으로 돌려 넣거나 `MouseEvent`를
`dispatchEvent`하는 방식으로 13개 시나리오(시무룩해지기·반기기 후 복귀·
작업 전환·건드리기·인사·고민·세션 전환 후 복귀·좌우 호버 전환·호버 해제 후
복귀 등)를 전부 확인했다.

(executeJavaScript로 여러 스니펫을 순서대로 실행할 때 `const`를 반복
선언하면 두 번째 호출부터 SyntaxError가 난다 — 페이지의 같은 전역
스코프를 매번 재사용하기 때문. IIFE로 감싸서 우회했다.)

### 6. ~~말풍선에서 답변 전체 보기~~ → 답변 본문 표시 자체를 없앰
답변이 도착하면 말풍선 아래에 본문을 5줄까지 펼쳐 보여주던 기능이
너저분하다는 피드백을 받아, `#bubble-body` 블록 자체를 삭제했다
(`renderer/index.html`, `renderer/renderer.js`의 `showReply`/`bodyEl`,
`renderer/style.css`). 말풍선 제목에는 여전히 `bubbleTitle()`이 답변/질문
요약을 짧게 보여주고, 답변이 막 도착하면 펫이 `jump`로 반기는 건 그대로
유지했다 — 텍스트만 없앴다.

### 7. ~~시트 눈·코 결함~~ → 완료 (원본을 새로 교체)
사용자가 실제 실행 화면에서 "눈알이 이상하다"고 지적해서 확인해보니,
`mong-sheet.webp`의 알파 채널에서 눈·코 부분이 완전히 투명(알파 0)하게
뚫려 있었다 — 배경 제거 도구가 어두운 픽셀(눈동자·코)을 배경으로 오인해서
지워버린, 시트 전체에 걸친 문제였다.

`tools/find-holes.js`(4방향 레이캐스팅으로 "실루엣 안쪽에 완전히 둘러싸인
투명 구멍"만 골라내는 도구, 실루엣 가장자리의 정상적인 반투명은 걸러냄)와
`tools/probe-pixel.js`(의심 좌표의 실제 RGBA 값을 직접 찍어보는 도구)를
새로 만들어 검증했다.

처음엔 `tools/fix-holes.js`(구멍을 4방향 최근접 색 중 가장 어두운 색으로
채우는 인페인팅 도구)로 땜질했는데, 사용자가 "그냥 원래 시트를 쓰면 되는
거 아니냐"고 다시 물어서 파일 시스템을 더 뒤져봤다. 다른 도구(Codex)로
같은 캐릭터를 계속 다듬어 온 작업 폴더(`Documents/Codex/...`)에서 구멍이
전혀 없는 리페어 완료본(`mong-v2-repair/final/mong-v2-edgefix.png`)을
찾아 잠깐 썼다가, 그 직후 사용자가 진짜 최종본(`spritesheet.webp`)을
프로젝트 폴더에 새로 올려줘서 최종적으로 그걸로 교체했다. 이번 최종본은
find-holes 기준으로도 남은 구멍이 미미하고(최대 126픽셀, 발밑 얼룩 정도),
확대해서 봐도 눈·코가 완전히 자연스럽다.

인페인팅(fix-holes)은 진짜 원본을 못 구했을 때 쓰는 최후 수단이므로
도구는 남겨뒀지만, 이번엔 안 썼다 — **시트에 눈에 띄는 결함이 있으면
먼저 "혹시 더 깨끗한 원본이 어딘가에 있는지" 확인하는 게 먼저**라는 교훈.

### 8. ~~듀얼 모니터로 드래그가 안 되던 것~~ → 완료
펫을 드래그해서 다른 모니터로 넘기려 하면 절대 못 넘어가는 버그가 있었다.
원인: `drag-window-by`가 "가장 가까운 디스플레이 하나"의 workArea로
clamp했는데, 그 판정 기준점이 창의 왼쪽-위 좌표(nx,ny)였다. 펫이 모니터
경계를 넘어가려는 순간에도 nx는 아직 원래 모니터 안에 있으므로, 매
mousemove마다 즉시 그 모니터 안으로 되돌려져 버려 다음 모니터 좌표에
영영 도달하지 못했다 — clamp가 자기 자신을 가두는 구조였다.

`clampPetVisible()`로 교체: "펫이 실제로 보이는 사각형이 어느
디스플레이와든 겹치기만 하면 이동을 허용"하는 방식으로 바꿨다. 화면
밖으로 완전히 나가려 할 때만 (펫 중심에서 가장 가까운 디스플레이 경계로)
clamp한다. 이 PC의 실제 3중 모니터 배치(주 모니터/왼쪽 세로 모니터/위쪽
모니터)에서 왼쪽·위쪽 모니터로 자유롭게 넘어가는 것과, 모니터가 없는
방향으로는 clamp되는 것을 순수 함수 시뮬레이션으로 확인했다.

### 9. Claude Code 실행 시 자동 실행
사용자가 "이게 클로드를 키면 자동으로 켜지는거여야해"라고 요청해서,
`SessionStart` 훅에서 펫 앱을 자동으로 띄우도록 만들었다.

- `main.js`의 `ensureDataDir()`가 앱을 실행할 때마다
  `~/.claude-pet/launch-info.json`에 `{execPath, args}`를 적어 둔다.
  `process.execPath`는 개발 모드(electron.exe)든 패키징된 앱이든 실제
  실행 파일을 정확히 가리키므로, 이것만 있으면 나중에 그대로 재실행할 수
  있다. 패키징된 앱(`app.isPackaged`)이면 `args: []`, 개발 모드면
  `args: [프로젝트 경로]`.
- `hooks/report-status.js`가 `hook_event_name === 'SessionStart'`를
  받으면 `launchPetAppIfNeeded()`를 호출해 `spawn(execPath, args,
  {detached, stdio:'ignore', windowsHide})`로 띄운다. 이미 떠 있으면
  앱의 `requestSingleInstanceLock`이 새 인스턴스를 조용히 즉시 종료시켜
  주므로, "떠 있는지 미리 확인"하지 않고 매번 그냥 실행을 시도해도
  안전하다. 이 환경 특유의 `ELECTRON_RUN_AS_NODE` 환경변수는 자식
  프로세스에 물려주지 않는다(있으면 electron이 순수 Node로 동작해 창이
  안 뜬다).
- 검증: 앱을 한 번 실행해 launch-info.json이 만들어지는지 확인 → 앱을
  강제 종료 → `SessionStart` 훅을 직접 실행해 자동으로 다시 뜨는지 확인
  → 이미 떠 있는 상태에서 훅을 또 실행해도 프로세스 개수가 늘지 않는지
  (중복 실행 안 됨) 확인. 전부 실제로 재현해서 통과했다.

### 10. ~~클립 우선순위 재구성~~ → 완료
이전 우선순위(세션 전환 시 걷기, 마우스 호버 시 애니메이션 재생,
working이 길어지면 poke, idle이 길어지면 sulky)를 사용자가 다시 다음과
같이 정의했다.

1. **펫을 드래그로 이동시키는 중** → 그 방향으로 걷기(`walkRight`/`walkLeft`).
   드래그 이동량(dx)의 부호로 방향을 정하고, 드래그가 끝나면 원래 상태로
   복귀한다. (세션 전환 시의 걷기 연출은 없앴다 — 이제 점 클릭은 그냥
   즉시 다음 세션으로 넘어간다.)
2. 답변 도착 → `jump` (그대로)
3. **마우스가 펫 가까이(주변) 있을 때** → `lookRight`/`lookLeft`를
   "재생"하지 않고, 마우스 각도에 정확히 대응하는 정지 프레임 하나를
   바로 그린다(`sprite.setStaticFrame`). row9는 0°~157.5°, row10은
   180°~337.5°를 22.5° 간격 8프레임씩 담고 있어 합쳐서 16방향 전체 원이
   된다. **단, 마우스가 펫 몸통 위에 정확히 있으면 오히려 쳐다보지 않고
   정면(그 상태의 기본 모션)을 유지한다** — "만지고 있을 땐 얌전히,
   다가오면 쳐다본다"는 느낌. 펫 사각형 가장자리에서 26px 이내(주변)일
   때만 반응한다 (`distanceToRect` + `HOVER_MARGIN_PX`).
4. 확인 필요 → `wave`, 생각 중 → `ponder` (그대로)
4. **작업 중 → `working`만** (더 이상 시간이 지나도 `poke`로 안 바뀐다)
4. **대기(idle) 중 → `idle` 기본, 20~40초마다 한 번씩 `poke`를 1.5초
   짧게 끼워 넣는다** (할 일이 없을 때 심심해서 몸을 긁적이는 느낌)
4. **세션이 아예 없을 때(할 게 없을 때) → `sulky`** (예전엔 idle이
   60초 넘게 이어지면 sulky였는데, 이제 "일이 없어서 시무룩"으로 조건을
   바꿨다)

`renderer/sprite.js`에 `setStaticFrame(name, frameIndex)`를 추가해서
(애니메이션 재생 중인 `setClip`과 별개로) 특정 프레임 하나에 고정할 수
있게 했다. 13개 시나리오(드래그 방향·답변 반기기·notify/thinking/working
전환·idle의 주기적 poke·세션 없음의 sulky·펫 위/주변 판정·16방향 각도
전부)를 앱을 띄운 채 시간 변수 직접 조작 + 실제 흐름과 동일한
`updateHover()` 호출로 검증했다.

### 11. ~~시트 마젠타 잔여물 제거~~ → 완료
새로 받은 시트에도 발밑 등에 마젠타(크로마키 배경) 잔여물이 남아 있어서
(`(189,4,193)`처럼 R,B가 G보다 훨씬 높은 픽셀), `tools/fix-magenta.js`를
새로 만들어 지웠다. R·B와 G의 차이가 임계값을 넘는 픽셀을 찾아 4방향
최근접 "깨끗한" 픽셀 색의 거리 역가중 평균으로 채우고, 못 찾으면
투명화한다. 처리 후 같은 도구로 재검사해 마젠타 픽셀 0개를 확인했다.

### 12. always-on-top이 특정 게임에 밀리는 문제 (부분 개선, 근본 한계 있음)
사용자가 펫을 다른 모니터로 옮겨서 확인해 달라고 했는데, 실제로 창이
Windows API 레벨에서는 정확한 위치에 정상 존재하는데도(`GetWindowRect`,
`WindowFromPoint`로 확인) 화면엔 안 보였다. 원인을 좁혀 보니 마비노기
(Unity 게임)가 그 좌표의 최상단 창을 계속 차지하고 있었다 —
`WindowFromPoint` + `GetAncestor(GA_ROOT)`로 실제 최상단 창을 확인해서
알아냈다.

시도한 것과 결과:
- `moveTop()` — 효과 없음
- `setAlwaysOnTop(false)` → `setAlwaysOnTop(true, 'screen-saver')` 토글,
  최대 200ms 주기까지 — 그래도 마비노기를 못 이김
- `focusable: true`로 전환(WS_EX_NOACTIVATE 제거) — 그래도 안 됨

즉 이 특정 게임은 Electron 표준 API로 이기기 어려울 만큼 적극적으로
자기 자신을 최상단으로 유지하고 있다. 게다가 발로란트처럼 **배타적
전체화면(exclusive fullscreen) 모드**로 켜져 있으면 애초에 어떤
오버레이도 못 뜬다(디스코드 오버레이도 마찬가지로 안 뜨는, DWM 합성을
우회하는 근본적 제약) — 이건 코드로 우회할 방법이 없다.

최종적으로는 실용적인 선으로 타협했다: `focusable: false`(원래 의도인
"다른 창 포커스 안 뺏기" 유지)로 되돌리고, `setAlwaysOnTop` 토글 주기를
2.5초로 완화했다. 평소 브라우저·문서 작업 등 일반 창들 사이에서는 이
정도로 충분히 맨 위를 유지할 것으로 보이며, 이런 특정 게임과 겹칠 때만
가려질 수 있다.

### 13. 아이콘 교체
`assets/icons/tray.png`, `app-icon.png`이 아직 예전 것이다. 몽이로 교체 필요.

### 14. 확인 안 된 것들
- Windows에서만 실행해 봤다. macOS / Linux 미확인.
- `npm run dist` (electron-builder 패키징) 미확인. 패키징된 앱에서
  자동 실행(9번 항목)의 `args: []` 분기도 아직 실제로는 못 돌려봤다.
- 세션이 3개 이상일 때 말풍선 아래 점 클릭 전환이 잘 도는지 미확인.
- 호버 반경(`HOVER_MARGIN_PX=26`)이 펫 크기를 아주 작게(50%) 줄였을 때도
  창 여백 안에 들어오는지 미확인 — 100% 배율에서는 좌우 여백이 104px라
  넉넉했다.

## 참고: 개발 중 걸렸던 것

이 환경에는 `ELECTRON_RUN_AS_NODE=1`이 설정돼 있어서 `npx electron .`이
GUI 없이 순수 Node로 돌아버린다. 앱이나 `tools/` 스크립트를 실행할 땐
아래처럼 환경 변수를 빼고 실행해야 한다.

```
env -u ELECTRON_RUN_AS_NODE npx electron .
```
