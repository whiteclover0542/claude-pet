# 남은 작업

요청한 5개 기능의 현재 상태와, 아직 못 한 것들을 정리해 둔다.

## 기능별 진행 상황

| # | 기능 | 상태 | 비고 |
|---|------|------|------|
| 1 | 캐릭터 커스텀 / 시트 애니메이션 | 대부분 완료 | 시트 등록이 아직 수동 (아래 참고) |
| 2 | 진행 중인 채팅 보여주기 | 완료 | 말풍선에 제목 + 상태 |
| 3 | VSCode 채팅 연동 | 완료 | `entrypoint`로 출처 구분, 세션별 파일 분리 |
| 4 | 답변 도착 시 말풍선에 표시 | 구현 완료 · 확인 미완 | 아래 참고 |
| 5 | 펫 크기 조절 | 완료 | 트레이 → 크기 (50~200%) |

## 아직 못 한 것

### 1. 답변 표시(기능 4) 실제 확인
훅이 트랜스크립트에서 답변을 뽑아내는 것까지는 검증했지만
(`hooks/report-status.js` → `reply` / `replyAt`), 말풍선에 실제로 펼쳐지는
화면은 아직 눈으로 확인하지 못했다. 앱을 켜고 Claude Code에서 한 턴 돌려서
확인 필요.

### 2. 커스텀 시트 등록 UI
지금은 새 캐릭터를 넣으려면 손으로 두 단계를 거쳐야 한다.

```
1) assets/characters/<이름>-sheet.webp 로 시트 이미지를 넣는다
2) npx electron tools/analyze-sheet.js     # 프레임 격자 자동 검출
   npx electron tools/preview-rows.js      # 어떤 행이 어떤 동작인지 눈으로 확인
   node tools/build-sheet-json.js          # assets/characters/<이름>.json 생성
```

`tools/build-sheet-json.js`의 `CLIPS` / `STATES` 상수를 시트에 맞게 고쳐야 한다.
→ 이걸 앱 안의 설정 창에서 "이미지 끌어다 놓기 → 행별 동작 고르기"로 할 수 있게
만드는 게 남았다.

### 3. 설정 창(GUI)
현재 설정은 전부 트레이 메뉴에만 있다. 크기 슬라이더, 캐릭터 미리보기,
말풍선 표시 시간 같은 걸 조절할 창이 없다.

### 4. 시트 원본 해상도로 교체
지금 들어 있는 `assets/characters/bichon-sheet.webp`는 대화에 붙여넣는 과정에서
**1536x2288 → 1343x2000으로 축소되고 손실 압축된 사본**이다. 원본 파일을
같은 경로에 덮어쓰고 위 2번 절차를 다시 돌리면 화질이 올라간다.

### 5. 안 쓰고 있는 클립 활용
시트에 있는 11개 동작 중 4개만 상태에 물려 있다.
남은 것: `run`(달리기), `beg`(조르기), `sleep`(엎드려 잠들기), `happy`(신남),
`sit`(앉아 방긋), `turn`(한 바퀴), `trot`(작게 총총).

쓸 만한 곳:
- 오래 대기하면 `sleep`으로 전환
- 답변이 막 도착했을 때 `happy` 한 번 재생
- 긴 작업이면 `walk` 대신 `run`

### 6. 말풍선에서 답변 전체 보기
지금은 5줄로 잘리고 20초 뒤 접힌다. 클릭하면 전체를 스크롤해서 보는 기능이 없다.

### 7. 아이콘 교체
`assets/icons/tray.png`, `app-icon.png`이 아직 예전 것이다. 비숑으로 교체 필요.

### 8. 확인 안 된 것들
- Windows에서만 실행해 봤다. macOS / Linux 미확인.
- `npm run dist` (electron-builder 패키징) 미확인.
- 세션이 3개 이상일 때 말풍선 아래 점 클릭 전환이 잘 도는지 미확인.

## 참고: 개발 중 걸렸던 것

이 환경에는 `ELECTRON_RUN_AS_NODE=1`이 설정돼 있어서 `npx electron .`이
GUI 없이 순수 Node로 돌아버린다. 앱이나 `tools/` 스크립트를 실행할 땐
아래처럼 환경 변수를 빼고 실행해야 한다.

```
env -u ELECTRON_RUN_AS_NODE npx electron .
```
